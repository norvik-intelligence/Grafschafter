"use client";
import { FormEvent, useState } from "react";

type State="idle"|"sending"|"success"|"error";
export default function Contact(){const [state,setState]=useState<State>("idle"); const [message,setMessage]=useState(""); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");setMessage("");const form=e.currentTarget;const data=Object.fromEntries(new FormData(form));try{const r=await fetch("/api/kontakt",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});const body=await r.json();if(!r.ok) throw new Error(body.error);setState("success");setMessage("Vielen Dank. Ihre Anfrage wurde versendet.");form.reset();}catch(error){setState("error");setMessage(error instanceof Error?error.message:"Ihre Anfrage konnte nicht versendet werden. Bitte versuchen Sie es später erneut.");}}return <section id="kontakt" className="contact section-pad"><div><p className="eyebrow"><span /> Kontakt</p><h2>Wir nehmen uns<br /><i>Zeit für Sie.</i></h2><p>Schildern Sie uns kurz Ihre Situation. Wir melden uns persönlich und besprechen gemeinsam, welche Unterstützung sinnvoll ist.</p><div className="contact-details"><a href="tel:+492841000000">+49 (0) 2841 000 000</a><a href="mailto:info@grafschafter-alltagsservice.de">info@grafschafter-alltagsservice.de</a><p>Lippestraße · 47441 Moers</p></div></div><form onSubmit={submit} noValidate><label>Ihr Name<input required name="name" type="text" maxLength={120} placeholder="Vor- und Nachname" /></label><label>Telefon oder E-Mail<input required name="contact" type="text" maxLength={200} placeholder="Wie dürfen wir Sie erreichen?" /></label><p className="form-note">Bitte tragen Sie hier keine Gesundheitsdaten, Diagnosen oder anderen besonders sensiblen Angaben ein.</p><label>Ihre Nachricht<textarea required name="message" rows={4} maxLength={4000} placeholder="Wobei dürfen wir Sie unterstützen?" /></label><label className="honeypot" aria-hidden="true">Website<input name="website" tabIndex={-1} autoComplete="off" /></label><label className="consent"><input required name="consent" type="checkbox" /> <span>Ich habe die Datenschutzhinweise gelesen und bin mit der Kontaktaufnahme einverstanden.</span></label><button className="button" type="submit" disabled={state==="sending"}>{state==="sending"?"Wird gesendet …":"Erstgespräch anfragen"} <span>↗</span></button><p className={`form-status ${state}`} role="status" aria-live="polite">{message}</p></form></section>;}
